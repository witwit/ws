{
  "private": true,
  "scripts": {
    "lint": "lerna run --stream -- ws lint",
    "build:production":
      "lerna run --stream --scope example-node-* -- ws build && lerna run --stream --ignore example-node-* -- ws build -- --production",
    "build": "lerna run --stream -- ws build",
    "unit": "lerna run --stream --ignore example-electron-* -- ws unit"
  },
  "workspaces": ["packages/*"],
  "resolutions": {
    "**/@types/react": "^15.0.0",
    "**/react": "^15.0.0"
  },
  "devDependencies": {
    "lerna": "2.5.1"
  }
}
